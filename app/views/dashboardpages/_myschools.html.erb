<h1 class="text-center my-1">Mes écoles</h1>
  <%# <input type="search" id="address-input" placeholder="Where are we studying?" /> %>
  <div class="main">
    <div class="cards">
      <% @devschools.each do |devschool| %>
          <div class="card-school">
            <%= devschool.name %><br/>
            <small><%= devschool.address %></small><br/>
            <i class="fas fa-hand-holding-usd"></i>
            <small><%= devschool.payment_facilities %></small>   
          </div>
            <% end %>
    </div>
    <div id="map"
      style="width: 100%; height: 450px;"
      data-markers="<%= @markers.to_json %>"
      data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
    </div>
  </div>
